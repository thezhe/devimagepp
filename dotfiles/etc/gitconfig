# Enable tag and commit signing with ssh key on a system level.
# See https://git-scm.com/docs/git-config.

[user]
	signingkey = ~/.ssh/id_rsa.pub
[gpg]
	format = ssh
[tag]
	gpgsign = true
[commit]
	gpgsign = true
